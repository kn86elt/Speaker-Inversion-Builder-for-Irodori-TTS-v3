[project]
name = "irodori-tts-v3-speaker-inversion-webui"
version = "0.1.0"
description = "WebUI for building speaker inversion datasets for Irodori-TTS v3."
requires-python = ">=3.10,<3.13"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "python-multipart>=0.0.12",
  "jinja2>=3.1.0",
  "pydub>=0.25.1",
  "soundfile>=0.12.1",
]

[tool.uv]
package = false
