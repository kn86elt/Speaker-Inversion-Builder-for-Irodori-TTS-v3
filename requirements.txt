# Legacy pip requirements for the WebUI.
# Prefer the uv-managed project environment:
#   uv sync --no-dev
#   uv run --no-sync python webui\app.py

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
jinja2>=3.1.0
pydub>=0.25.1
soundfile>=0.12.1
